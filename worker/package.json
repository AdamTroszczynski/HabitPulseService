{
  "name": "@habitpulse/worker",
  "version": "1.0.0",
  "description": "",
  "main": "src/worker.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../.env src/worker.ts",
    "build": "tsup --config ./tsup.config.ts",
    "preview": "npm run build && node dist/worker.js",
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "format": "prettier --write \"./**/*.+(js|ts|json|md)\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^8.0.2"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.11"
  }
}
