commit-msg:
  commands:
    commitlint:
      run: server/node_modules/.bin/commitlint --edit {1}

pre-commit:
  commands:
    lint-staged:
      run: server/node_modules/.bin/lint-staged --config server/package.json
      cwd: server